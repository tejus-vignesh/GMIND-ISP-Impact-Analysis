# Core dependencies for GMIND SDK
# Compatible with Python 3.10+
#
# For installation options, see pyproject.toml or use:
#   pip install -e .                    # Core dependencies only
#   pip install -e ".[ultralytics]"     # With YOLO support
#   pip install -e ".[eval]"             # With evaluation tools
#   pip install -e ".[dev]"              # With development tools
#   pip install -e ".[all]"              # All optional dependencies

# PyTorch and Computer Vision
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21
opencv-python>=4.8
scipy>=1.7

# Utilities
tqdm>=4.62
PyYAML>=6.0
Pillow>=9.0
matplotlib>=3.5.0
pandas>=1.3.0

# Evaluation (recommended for Evaluation module)
pycocotools>=2.0.2

# Optional backends (install as needed)
# ultralytics>=8.0.0       # For YOLO variants (Annotation, Evaluation, DeepLearning)
# mmdet>=3.0.0             # For MMDetection models
# mmcv>=2.0.0rc4,<2.2.0    # Required by MMDetection
# tensorboard>=2.10.0      # For training visualisation (optional)
# PyQt5>=5.15.0            # For Calibration GUI (LIDAR-camera extrinsic calibration)

# Development and testing (optional)
# pytest>=7.0
# pytest-cov>=4.0
# black>=23.0
# isort>=5.12
# pylint>=2.17
# mypy>=1.0
